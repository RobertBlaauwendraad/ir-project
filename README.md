# IR Project

Implementation of the Build an IR system from Scratch project.

## Requirements

- Python >= 3.12

## Installation

```bash
pip install -r requirements.txt
```

Or using uv:

```bash
uv sync
```
